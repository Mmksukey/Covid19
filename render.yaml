services:
  - type: web
    name: covid-risk-predictor
    env: python
    rootDir: web
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
